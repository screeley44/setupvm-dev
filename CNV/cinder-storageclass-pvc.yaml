kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-clone
  annotations:
spec:
  storageClassName: standalone-cinder
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
