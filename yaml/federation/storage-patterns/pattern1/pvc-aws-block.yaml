apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aws-block-claim
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 100Gi
  storageClassName: aws-block

